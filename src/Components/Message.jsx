/* eslint-disable react/prop-types */

const Message = ({ name, course }) => {
  return (
    <div>
        <h4>Hola, {name}!!! Sabemos que estás en el curso {course} </h4>
    </div>
  )
}

export default Message